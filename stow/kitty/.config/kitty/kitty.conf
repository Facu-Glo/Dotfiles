# =============================================================================
# kitty.conf — Main entry point
# =============================================================================
# Loads all configuration modules in order. If two files set the same option,
# the last one loaded wins.
#
# All module files live in conf/*.conf.
# =============================================================================

include conf/misc.conf
include conf/window.conf
include conf/tab-bar.conf
include conf/keyboard.conf
include conf/current-theme.conf
include conf/fonts.conf
